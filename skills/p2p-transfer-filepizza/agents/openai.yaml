interface:
  display_name: "p2p-transfer-filepizza"
  short_description: "Share local files through public FilePizza"
  default_prompt: "Use $p2p-transfer-filepizza to create, inspect, and stop public file.pizza uploads for local files through the bundled Playwright wrapper."
