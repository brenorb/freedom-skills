interface:
  display_name: "p2p-transfer-filepizza"
  short_description: "Share local files using FilePizza CLI"
  default_prompt: "Use $p2p-transfer-filepizza to share a local file, inspect its status, or stop seeding with filepizza-cli via npx."
