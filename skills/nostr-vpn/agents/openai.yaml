interface:
  display_name: "nostr-vpn"
  short_description: "Nostr VPN CLI for mesh setup, joins, and diagnostics"
  default_prompt: "Use $nostr-vpn to locate the local `nvpn` executable (including bundled desktop-app CLIs), inspect Nostr VPN state, handle join requests, manage sessions and services, and troubleshoot safely without exposing secrets."
